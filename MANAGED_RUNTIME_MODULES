# Managed runtime modules required in every install/update/release surface.
# One module name per line. This file is the canonical declaration of what the
# installed distribution must provide before any behavior is moved out of the
# cockpit_control facade.

cockpit_control
cockpit_control_root_schema
cockpit_control_locks
cockpit_control_journal
cockpit_control_projection
