---
name: e2e-cockpit
description: "@@APP_NAME@@ Playwright E2E cockpit: project topology overlay — cockpit windows, worker roles, test runner, connection details, triage notes. USE FOR: running Playwright tests against @@APP_NAME@@, reading logs, interpreting failures, reporting to the overseer."
---

# E2E Cockpit — @@APP_NAME@@ (Project Topology Overlay)

> Load the **global** `e2e-cockpit` skill first for the generic overseer protocol,
> dispatch patterns, question protocol, and triage workflow.
> This file adds the `@@APP_NAME@@`-specific topology on top.
> Use `cockpit-overseer loop` for compact status checks instead of re-capturing
> full panes in a loop.

> **VP3 control store:** Before controller work, use the launcher-exported
> absolute `COCKPIT_CONTROL_ROOT` and run `cockpit-overseer start`. The control
> root may only fall back to the active tmux session when absent from the shell;
> never derive it from the current directory.

> **If you are in `worker-test`** — load the `e2e-operator` skill instead.

## Cockpit communication protocol

When a tmux cockpit is running, treat it as managed state. Use
`cockpit-protocol` for all cockpit pane communication and observability
(`dispatch`, `send`, `tail`, `watch`, `pending`, `read-question`, `reply`,
`meta cockpit --json`, `status --workers all --json`).

Use `cockpit-overseer status` / `cockpit-overseer loop` for compact status
checks and short-loop polling.

Do not inspect or control the cockpit with ad-hoc raw `tmux` commands. This
applies to read-only status requests as well as worker coordination. Do not
improvise `tmux ls`, `tmux list-windows`, `tmux list-panes`,
`tmux capture-pane`, `tmux load-buffer`, `tmux paste-buffer`, or
`tmux send-keys` unless the user explicitly asks for raw tmux diagnostics.

If the parent repo has `graphify-out/graph.json` and `graphify` is available,
use `graphify query "<question>" --graph "$REPO/graphify-out/graph.json"`
before broad text search for codebase, architecture, file-relationship, and
project-content questions. Pass the graph path in worker briefs when it helps
orientation.

---

## ── CONFIGURE ── Cockpit topology

This section is regenerated by `/setup-e2e-cockpit` once the app topology is discovered.
Until then, the generic windows below apply (see `tmux-cockpit.sh`).

| tmux window   | Role |
|---------------|------|
| `overseer`    | Orchestrates workers, reads results, delegates |
| `k8s-logs`    | Live log tails from the app's backend/services |
| `k8s-pf`      | Port-forward → `localhost:@@BACKEND_PORT@@` — must stay alive |
| `chromium`    | Playwright browser (CDP) |
| `worker-test` | E2E Test Operator — `e2e-operator` skill |
| `worker-dev`  | Developer — `worker-dev` skill |
| `worker-fix`  | Troubleshooter — `worker-fix` skill |

The **local dev cockpit** (`tmux-cockpit-local.sh`) replaces the remote log/port-forward
windows with a `backend` window running the app locally on `:@@BACKEND_PORT@@`.

---

## ── CONFIGURE ── Connection

| Resource | Value |
|----------|-------|
| Frontend | `http://localhost:@@FRONTEND_PORT@@` |
| Backend  | `http://localhost:@@BACKEND_PORT@@` |
| Health check | `curl -sk http://localhost:@@BACKEND_PORT@@@@HEALTH_PATH@@` |

---

## Running tests

```bash
# From the e2e/ directory
./run-audit.sh --scope "@smoke"   # smoke gate (always first)
./run-audit.sh                    # full suite
./run-playwright.sh --grep @smoke # ad-hoc
```

For overseer status, prefer:

```bash
cockpit-overseer loop --session "<session>" --window worker-test --window worker-dev --window worker-fix
```

Prerequisites:
1. Backend reachable → `curl -sk http://localhost:@@BACKEND_PORT@@@@HEALTH_PATH@@` returns 200
2. `e2e/.env.local` with `FRONTEND_URL`, `BACKEND_URL`, `SKIP_DB_RESET=true`
