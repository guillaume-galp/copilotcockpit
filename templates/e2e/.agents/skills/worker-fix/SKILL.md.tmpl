---
name: worker-fix
description: "@@APP_NAME@@ Codex troubleshooter overlay. Load the global role skill first, then apply project-specific investigation commands."
---

# worker-fix — @@APP_NAME@@ Codex overlay

Load the global role skill `$worker-fix` first, then apply this project extension.

Use `cockpit-protocol` for pane communication and worker question/answer handoffs.

If the parent repo has `graphify-out/graph.json` and `graphify` is available,
use `graphify query "<question>" --graph "$REPO/graphify-out/graph.json"`
before broad text search to map failing behavior to related code, architecture,
file relationships, and likely blast radius.

## Repo layout

- Root: `@@APP_NAME@@`
- Logs and artifacts: `e2e/`, `e2e/runs/`, `e2e/governance/`

## Test runner

```bash
./run-audit.sh --scope "@smoke"
```

## Health and services

- Health: `<health-url>`
- Namespace: `<k8s-namespace>`

## Worker question file

- Question file: `/tmp/worker-worker-fix-question.txt`
